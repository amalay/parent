name: CI/CD - Parent

on:  
  push:
    branches: [ "main" ]
  
  workflow_dispatch:

jobs:  
  build:    
    runs-on: ubuntu-latest
    
    steps:      
      - uses: actions/checkout@v3
      
      - name: Print
        run: |
          echo Execute parent ci/cd pipeline.
          echo Testing parent ci/cd pipeline.
